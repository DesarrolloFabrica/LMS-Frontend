/**
 * Reservado por si en el futuro se necesita un puente visual entre secciones.
 * El dashboard usa ahora un único canvas claro continuo (sin corte entre hero y contenido).
 */
export function SectionTransition() {
  return null;
}
